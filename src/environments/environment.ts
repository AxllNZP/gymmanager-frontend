export const environment = {
  production: false,

  // URL del backend en desarrollo local.
  // El interceptor auth.interceptor.ts agrega el Bearer token a cada petición.
  apiUrl: 'http://localhost:8080/api'
};
